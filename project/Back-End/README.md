only change the files in cgi/user. The rest should be handled by linhai due to their importance in the server setup.

Replace the files with their identical counterparts in Dev/ to get the server running on localhost instead of www.yinlinhai.com
